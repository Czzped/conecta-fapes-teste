image: node:21

cache:
  paths:
    - documentation/node_modules/
    - documentation/public/
    - documentation/.cache/

pages:
  stage: deploy
  script:
  - cd documentation
  - rm -rf public
  - yarn install
  - yarn build:gitlab

  artifacts:
    paths:
      - documentation/public
  only:
    - main
