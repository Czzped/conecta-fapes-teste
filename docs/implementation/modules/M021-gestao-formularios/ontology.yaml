ontology: "Gestão de Formulários"
namespace: "suporte.formularios"
imports:
  - namespace: shared
    path: path: "../../../ontology/shared/base.yaml"
metadata:
  module: "M021"
  source: "stub — no modelo-estrutural.md available"
  version: "1.0.0"
  description: "Gestao de formularios dinamicos. Utilizado por M011 (configuracao de captacao) para formularios de submissao e avaliacao de projetos e bolsas."
  todo: "Complete when modelo-estrutural.md is created"
entities:
  todo: "Define when modelo-estrutural.md is available"
relationships:
  todo: "Define when modelo-estrutural.md is available"
axioms:
  todo: "Define when modelo-estrutural.md is available"
invariants:
  - id: RN28
    description: "Qualquer funcionario da FAPES autenticado pode criar um formulario e qualquer usuario logado pode criar uma resposta a formulario."
  - id: RN29
    description: "Um formulario ou uma resposta a formulario so pode ser editado pelo proprio usuario autor do registro, respeitadas as restricoes de estado aplicaveis."
  - id: RN30
    description: "Qualquer funcionario da FAPES autenticado pode criar um formulario a partir de qualquer formulario existente, independentemente da autoria do formulario original."
  - id: RN31
    description: "Qualquer funcionario da FAPES autenticado pode criar, consultar, atualizar, excluir e listar categorias de formulario, respeitadas as demais regras aplicaveis."
enums: {}
value_objects: {}
events:
  todo: "Define when modelo-estrutural.md is available"
workflows:
  todo: "Define when modelo-estrutural.md is available"
policies:
  permissionamento_autoria:
    applies_to:
      - Formulario
      - RespostaFormulario
    rules:
      - "Criacao de formulario exige funcionario da FAPES autenticado."
      - "Criacao de resposta a formulario exige usuario logado."
      - "Edicao exige que o usuario solicitante seja o autor do registro."
      - "Copia de formulario nao exige autoria do formulario original."
  permissionamento_categorias:
    applies_to:
      - CategoriaFormulario
    rules:
      - "Funcionalidades de categorias exigem funcionario da FAPES autenticado."

agent_instructions:
  rules:
    - "Este módulo é stub. Não gerar implementações sem modelo-estrutural.md."
    - "Consultar arquitetura em docs/architecture/02-modulos-e-integracoes.md."
  notes:
    - "Gestão de formulários dinâmicos."
    - "Usado por M011 (captacao) para formulários de submissão e avaliação."
