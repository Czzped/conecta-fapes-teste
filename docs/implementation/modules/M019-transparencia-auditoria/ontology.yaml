ontology: "Transparência e Auditoria"
namespace: "suporte.transparencia"
imports:
  - namespace: shared
    path: path: "../../../ontology/shared/base.yaml"
metadata:
  module: "M019"
  source: "stub — no modelo-estrutural.md available"
  version: "1.0.0"
  description: "Transparencia e auditoria. Portal de dados abertos e log de auditoria cross-modulo. Downstream de M005 (autenticacao) e M018 (BI)."
  todo: "Complete when modelo-estrutural.md is created"
entities:
  todo: "Define when modelo-estrutural.md is available"
relationships:
  todo: "Define when modelo-estrutural.md is available"
axioms:
  todo: "Define when modelo-estrutural.md is available"
invariants:
  todo: "Define when modelo-estrutural.md is available"
enums: {}
value_objects: {}
events:
  todo: "Define when modelo-estrutural.md is available"
workflows:
  todo: "Define when modelo-estrutural.md is available"
policies:
  todo: "Define when modelo-estrutural.md is available"

agent_instructions:
  rules:
    - "Este módulo é stub. Não gerar implementações sem modelo-estrutural.md."
    - "Consultar arquitetura em docs/architecture/02-modulos-e-integracoes.md."
  notes:
    - "Transparência e auditoria."
    - "Portal de dados abertos."
    - "Log de auditoria cross-módulo."
