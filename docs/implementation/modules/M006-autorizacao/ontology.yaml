ontology: "Autorizacao — OpenFGA RBAC"
namespace: "auth.autorizacao"
imports: []
metadata:
  module: "M006"
  source: "stub — no modelo-estrutural.md available"
  version: "1.0.0"
  description: "Autorização de acesso via OpenFGA (motor PDP). Avalia políticas RBAC/ABAC em tempo real para todos os módulos do ConectaFAPES."
  todo: "Complete when modelo-estrutural.md is created"

entities:
  todo: "Define entities when modelo-estrutural.md is available"

relationships:
  todo: "Define when modelo-estrutural.md is available"

axioms:
  todo: "Define when modelo-estrutural.md is available"

invariants:
  todo: "Define when modelo-estrutural.md is available"

enums: {}

value_objects: {}

events: []

workflows:
  todo: "Define when modelo-estrutural.md is available"

policies:
  todo: "Define when modelo-estrutural.md is available"

agent_instructions:
  rules:
    - "Este módulo é stub. Não gerar implementações sem modelo-estrutural.md."
    - "Consultar arquitetura em docs/architecture/02-modulos-e-integracoes.md."
  notes:
    - "Autorização via OpenFGA (RBAC)."
    - "Papéis inferidos de outros módulos: Coordenador, Bolsista, Avaliador, Gestor, Admin."
